import { makeStyles } from "@material-ui/core/styles";

export const useStyle = makeStyles ({
    root: {
        width: "100%",
        border: "1px dotted #000",
    },
    child: {
        width: "100%",
    }
})